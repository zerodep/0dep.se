{
  "name": "zerodep-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Landing page for the zerodep org and the bpmn-engine ecosystem (zerodep.se / 0dep.se).",
  "scripts": {
    "build": "node src/build.js",
    "test": "node --test test/*.test.js",
    "serve": "node src/serve.js"
  },
  "engines": {
    "node": ">=26"
  },
  "license": "MIT",
  "author": "Pål Edman",
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
